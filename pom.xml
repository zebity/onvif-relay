<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">  
  <modelVersion>4.0.0</modelVersion>
  <groupId>onvif-relay</groupId>
  <artifactId>onvif-relay</artifactId>
  <version>0.0.1-SNAPSHOT</version>
  <name>onvif-relay</name>
  <description>onvif-relay</description>
  <properties>
   <project.download.dir>${project.basedir}/target/generated-sources/wget/</project.download.dir>
   <project.wsdl.resource.dir>${project.basedir}/src/main/resources/META-INF/wsdl/</project.wsdl.resource.dir>
   <project.patch.dir>${project.basedir}/src/main/patch/</project.patch.dir>
   <project.sh.dir>${project.basedir}/src/main/sh/</project.sh.dir>

   <project.javax.annotations>2.3.1</project.javax.annotations>
   <project.javax.ver>2.3.1</project.javax.ver>
   <project.javax.jaxws-rt>2.3.5</project.javax.jaxws-rt>
   <project.jaxb.ver>3.0.2</project.jaxb.ver>
   <!-- project.javax.jaxws-rt>3.0.2</project.javax.jaxws-rt -->
   <project.jakarta.annotations>3.0.1</project.jakarta.annotations>
   <project.jakarta.ver>3.0.0</project.jakarta.ver>
   <!-- project.jetty.ver>11.0.12</project.jetty.ver -->
   <project.jetty.ver>10.0.12</project.jetty.ver>
   
   <project.jaxws-plugin.ver>${project.javax.jaxws-rt}</project.jaxws-plugin.ver>

  </properties>

  <!-- WSIMPORT DEPENDENCIES -->
  <dependencies>
    <dependency>
     <groupId>javax.xml.ws</groupId>
     <artifactId>jaxws-api</artifactId>
     <version>${project.javax.annotations}</version>
    </dependency>
    <!-- dependency>
     <groupId>jakarta.xml.ws</groupId>
     <artifactId>jakarta.xml.ws-api</artifactId>
     <version>${project.jakarta.annotations}</version>
    </dependency -->
    <dependency>
     <groupId>com.sun.xml.ws</groupId>
     <artifactId>jaxws-rt</artifactId>
     <version>${project.javax.jaxws-rt}</version>
     <scope>runtime</scope>
    </dependency>

    <!-- JAVAX BUILD DEPENDENCIES -->
    <dependency>
    	<groupId>com.sun.xml.bind</groupId>
    	<artifactId>jaxb-core</artifactId>
    	<version>${project.jaxb.ver}</version>
    </dependency>
    <dependency>
    	<groupId>com.sun.xml.bind</groupId>
    	<artifactId>jaxb-impl</artifactId>
    	<version>${project.jaxb.ver}</version>
    </dependency>
    <dependency>
     <groupId>javax.xml.bind</groupId>
     <artifactId>jaxb-api</artifactId>
     <version>${project.javax.ver}</version>
    </dependency>
    <dependency>
	 <groupId>javax.jws</groupId>
	 <artifactId>javax.jws-api</artifactId>
	 <version>1.1</version>
	</dependency>
    <!-- JAKARTS BUILD DEPENDENCIES -->
    <!-- dependency>
     <groupId>com.sun.xml.messaging.saaj</groupId>
     <artifactId>saaj-impl</artifactId>
     <version>3.0.0</version>
     <scope>runtime</scope>
    </dependency>
    <dependency>
    	<groupId>jakarta.xml.bind</groupId>
    	<artifactId>jakarta.xml.bind-api</artifactId>
    	<version>3.0.1</version>
    </dependency>
    <dependency>
    	<groupId>jakarta.jws</groupId>
    	<artifactId>jakarta.jws-api</artifactId>
    	<version>3.0.0</version>
    </dependency>
    <dependency>
    	<groupId>jakarta.xml.soap</groupId>
    	<artifactId>jakarta.xml.soap-api</artifactId>
    	<version>3.0.0</version>
    </dependency>
    <dependency>
    	<groupId>jakarta.servlet</groupId>
    	<artifactId>jakarta.servlet-api</artifactId>
    	<version>4.0.4</version>
    </dependency -->

    <dependency>
     <groupId>org.apache.commons</groupId>
     <artifactId>commons-configuration2</artifactId>
     <version>2.8.0</version>
    </dependency>
  	
    <dependency>
    	<groupId>org.eclipse.jetty</groupId>
    	<artifactId>jetty-webapp</artifactId>
    	<version>${project.jetty.ver}</version>
    </dependency>
        <dependency>
    	<groupId>org.eclipse.jetty</groupId>
    	<artifactId>jetty-server</artifactId>
    	<version>${project.jetty.ver}</version>
    </dependency>
    <dependency>
    	<groupId>org.eclipse.jetty</groupId>
    	<artifactId>jetty-http-spi</artifactId>
    	<version>${project.jetty.ver}</version>
    </dependency>

  </dependencies>

  <build>
   <plugins>

    <plugin>
     <groupId>org.apache.maven.plugins</groupId>
     <artifactId>maven-compiler-plugin</artifactId>
     <version>3.10.0</version>
    </plugin>

    <!-- plugin>
     <artifactId>maven-antrun-plugin</artifactId>
     <version>1.7</version>
     <executions>
      <execution>
       <phase>generate-sources</phase>
       <configuration>
        <tasks>
         <exec dir="${project.basedir}"
               executable="${project.sh.dir}wget-and-patch.sh"
               failonerror="true">"
          <arg line="${project.sh.dir}file.txt ${project.download.dir} ${project.wsdl.dir} ${project.patch.dir}" /> 
         </exec>
        </tasks>
       </configuration>
       <goals>
        <goal>generate-sources</goal>
       </goals>
      </execution>
     </executions>
    </plugin -->

    <plugin>
     <groupId>com.sun.xml.ws</groupId>
     <artifactId>jaxws-maven-plugin</artifactId>
     <version>${project.jaxws-plugin.ver}</version>
     <executions>
      <execution>
       <goals>
        <goal>wsimport</goal>
       </goals>
      </execution>
     </executions>
     <configuration>
       <wsdlLocation>/META-INF/wsdl/www.onvif.org/ver10/onvif_device.wsdl</wsdlLocation>
       <wsdlDirectory>${project.basedir}/src/main/resources/META-INF/wsdl/www.onvif.org/ver10</wsdlDirectory>
       <wsdlFiles>
        <wsdlFile>onvif_device.wsdl</wsdlFile>
       </wsdlFiles>
       <!-- The extension property tells JAX-WS to support soap v1.2 bindings. -->
       <extension>true</extension>
     </configuration>
    </plugin>
  
    <plugin>
     <groupId>org.codehaus.mojo</groupId>
     <artifactId>build-helper-maven-plugin</artifactId>
     <version>3.3.0</version>
     <executions>
      <execution>
       <id>add-source</id>
       <phase>generate-sources</phase>
       <goals>
         <goal>add-source</goal>
       </goals>
       <configuration>
        <sources>
         <source>${basedir}/src/main/java</source>
        </sources>
       </configuration>
      </execution>
     </executions>
    </plugin>

    <plugin>
      <groupId>org.eclipse.jetty</groupId>
      <artifactId>jetty-maven-plugin</artifactId>
      <version>${project.jetty.ver}</version>
    </plugin>

   </plugins>
  </build>
</project>
